export const SUPPORTED_AGGREGATES = ["SUM", "COUNT", "AVG", "MAX", "MIN"] as const;
export const DEFAULT_EPSILON = 1.0;
export const DEFAULT_PRIVACY_BUDGET = 10.0;
export const MIN_QUORUM = 2;
export const COORDINATOR_PORT = 4000;
